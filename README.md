Installation information

1. I have used a MySql database. In order to run the application you must update the connection string in the application.properties file.  
   Update this configuration spring.datasource.url=jdbc:mysql://localhost:3306/devices?createDatabaseIfNotExist=true
    